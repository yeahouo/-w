# 模式一 — v6 循迹调参存档 (2026-07-30)

## 控制参数

| 参数 | 值 |
|------|-----|
| LINE_FOLLOW_VERSION | 6 |
| 控制频率 | 80Hz (12.5ms/帧) |
| 基础速度 | 9 (45% duty) |
| V6_DT_FAST_MS | 67ms |
| V6_DT_SLOW_MS | 200ms |
| V6_CORRECT_ESCALATE | 2 |
| V6_TURN_PULSE_MS | 73ms |

## 场景速查

| 场景 | 触发 | 脉冲 | 阻尼 |
|------|------|------|------|
| s1 直行 | M 活跃, 最外无 | `(9,9)` | — |
| s2 丢线 | bits==0 | `(7,9)` diff=2 | — |
| s3 回正 | M + err=±1 | `(7,9)` diff=2, 67ms | `(9,9)` |
| s4 大转 | 无M 或 err=±2 | `(4,9)` diff=5, 73ms | ±2→5 / ±1→6 |

## 关键修复

- `err = (int8_t)(-err)` 已删除 (修左右反)
- 方向锁时仍更新 `s_v6_last_err` (防丢线用旧数据)
- M 活跃即刷新 dt (保证脉冲窗有效, 不吞场景 3)
- 停车: `MotorDrive_Brake()` TB6612 短接制动锁死
